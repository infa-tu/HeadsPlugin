# 封头 AutoCAD 自动绘图插件 — 规格说明 v1.1（已定稿）

## 1. 总体设计

| 项目 | 内容 |
|---|---|
| 语言/框架 | C++ / ObjectARX 2026 |
| 目标平台 | AutoCAD 2026 x64 |
| 插件形式 | .arx，`APPLOAD` 加载 |
| 主命令 | `DRAW_HEAD` |
| 输入方式 | MFC 模态对话框，对话框内下拉/单选选择封头类型 |
| 出图内容 | 轴对称**整个剖面（全剖视图）**（中心线两侧完整轮廓）+ 中心线 + 剖面线 + 自动尺寸标注 |
| 单位 | mm |
| 插入点 | 中心线与直边底端（下表面）交点；命令运行时提示用户捕捉基准点 |

## 2. 坐标系

- 中心线：x = 0，竖直，红色 CENTER 点划线
- 图形画在中心线**两侧**（左右对称，完整剖面）
- 下表面（直边底端）y = 0，向上为 +y
- 顶/底端面在剖视图中画成水平线

## 3. 图层规范（已确认）

| 用途 | 图层名 | 颜色 | 线型 |
|---|---|---|---|
| 轮廓 | HEAD_CONTOUR | 白色 ACI 7 | Continuous |
| 中心线 | HEAD_CENTER | 红色 ACI 1 | CENTER |
| 剖面线 | HEAD_HATCH | 绿色 ACI 3 | Continuous |
| 尺寸标注 | HEAD_DIM | 青色 ACI 4 | Continuous |

## 4. 碟形封头

### 输入参数
| 参数 | 含义 |
|---|---|
| Di | 内径 |
| Ri | 球冠（底部）内半径 |
| ri | 过渡圆弧（折边）内半径 |
| T | 壁厚 |
| L | 直边高度 |

### 几何构造（内轮廓，R = Di/2）
1. 直边段：x = R，y ∈ [0, L]
2. 折边圆弧：圆心 (R − ri, L)，半径 ri，与直边在 (R, L) 相切
3. 球冠圆弧：圆心 (0, yc)，半径 Ri，与折边圆弧内切；yc = L − √((Ri − R)(Ri + R − 2ri))
4. 顶点 (0, yc + Ri)
5. 外轮廓 = 内轮廓向外等距偏移 T

### 校验与自动调整（已确认）
- 相切条件：Ri ≥ Di/2、0 < ri ≤ Di/2
- 不满足 → 报错；勾选"自动调整"：
  1. 若 ri > Di/2 → ri = 0.1·Di
  2. 若 Ri < Di/2 → **Ri = Di**（标准球冠半径，保证折边有效）

### 标注
`ØDi`、`R Ri`、`R ri`、`T`、`L`

---

## 5. 1:2 椭圆封头

### 输入参数
| 参数 | 含义 |
|---|---|
| Di | 内径 |
| T | 壁厚 |
| L | 直边高度 |

### 几何构造（内轮廓）
- 椭圆：长轴（水平）= Di → a = Di/2；短轴（垂直）= Di/2 → b = Di/4
- 封头深度 = **Di/4**（标准 2:1 椭圆封头）
- 直边段：x = Di/2，y ∈ [0, L]
- 内椭圆：中心 (0, L)，上半椭圆 (x/a)² + ((y−L)/b)² = 1，y ≥ L
- 外轮廓 = **新椭圆**（工程制图惯例）：中心 (0, L)，半长轴 Di/2+T、半短轴 Di/4+T
- 外顶点 (0, L + Di/4 + T)；外直边 x = Di/2 + T

### 校验
- Di > 0、T > 0、L ≥ 0

### 标注
`ØDi`、`T`、`L`

---

## 6. 锥体类（CSA / CNA / CDA / CHD）

### 6.0 公共约定
| 参数 | 含义 |
|---|---|
| D | 顶部最大**外径** |
| Ds | 底部直边段**外径** |
| δn | 壁厚 |
| α | **外壁**与中心线的半锥角（α 输入，H 自动算出）|
| hs | 底部直边高度（统一加入，默认 25mm）|

- 方向：顶部大 → 底部小（倒梯形）
- 外轮廓由 D、Ds、α 确定；**内轮廓 = 外轮廓向内偏移 δn**
- 斜壁垂直高 h_cone = (D − Ds)/(2·tan α)（含圆角时为斜壁切点间垂直高，由相切关系确定）
- 顶/底端面为水平切口
- 校验：D > Ds > 0、0 < α < 90°（建议 ≤ 60°）、δn > 0、hs ≥ 0

### 6.1 CNA（无折边锥壳）
- 顶部：水平切口（无直边、无圆弧）
- 底部：直边 hs，**无圆角（尖角）**
- 外轮廓：底直边(x=Ds/2, 0≤y≤hs) → 斜壁(过底角(Ds/2,hs)，角度 α) → 顶面水平线
- H = hs + h_cone（自动算出）

### 6.2 CSA（带折边锥壳）
- 顶部：竖直短圆筒直边（外径 D、高 h）
- 顶部过渡：**内凹**圆弧 r（圆心在材料内侧），与顶部直边、斜壁相切
- 底部：直边 hs，无圆角（尖角）
- 外轮廓：底直边 → 斜壁(过底角，角度 α) → 顶部内凹圆弧 r → 顶部直边(D/2, h) → 顶面
- H = 自动算出（= 底直边 + 斜壁 + 折边 + 顶直边）
- 校验：r 过大导致与直边/斜壁无法相切 → 报错

### 6.3 CDA（带顶部圆角锥壳）（已确认几何）
- 顶部：**竖直向上翻边**（类似向上短圆筒），翻边高度 h，外径 D（翻边最外缘）
- 顶部圆角 r₁：连接斜壁与竖直翻边的**外凸**圆角（圆心在材料外侧）
- 主体：斜壁，半锥角 α
- 底部：**外凸**过渡圆角 rs → 底部直边(hs, Ds)
- 外轮廓：底直边(x=Ds/2,0≤y≤hs) → 底部圆角 rs(圆心外侧,与底直边/斜壁相切) → 斜壁(角度 α) → 顶部圆角 r₁(圆心外侧,与斜壁/翻边相切) → 竖直翻边(D/2, h) → 顶面
- H₀ = 自动算出
- 校验：α、r₁、rs、D、Ds 组合需满足相切（不满足报错）

### 6.4 CHD（简单锥形）（已确认几何）
- 顶部：**水平切口**（无翻边、无圆角）
- 底部：**外凸**过渡圆角 rs → 底部直边(hs, Ds)
- 外轮廓：底直边(x=Ds/2,0≤y≤hs) → 底部圆角 rs(圆心外侧,与底直边/斜壁相切) → 斜壁(角度 α) → 顶面水平线(在斜壁达到 x=D/2 处)
- H₀ = 自动算出

---

## 7. 尺寸标注规范

格式（带变量名）：
- 直径：`ØDi = 1000`
- 半径：`Rri = 150`
- 角度：`α = 30°`
- 厚度/高度：`T = 10`、`L = 40`、`H = 500`

图层 HEAD_DIM（青 4），尺寸位置/箭头样式实现时按国标微调。

---

## 8. 开发计划（由简到繁）

1. **阶段1**：工程框架 + MFC 对话框 + 图层管理 + 碟形封头（跑通"输入→计算→绘图→剖面线→标注"全链路）
2. **阶段2**：椭圆封头（精确等距偏移曲线）
3. **阶段3**：锥体 CNA → CSA → CHD → CDA

---

## 9. 关键决议记录（已全部确认）

1. 锥体 D/Ds 为外径，内轮廓 = 外轮廓向内偏移 δn
2. 以 α 为输入，H/H₀ 自动算出并标注
3. 所有锥体统一加底部直边高度 hs（默认 25mm）
4. α 指外壁与中心线夹角
5. 椭圆封头深度 = Di/4
6. 碟形自动调整：ri 优先不变；Ri 不足时取 **Di**；ri > Di/2 时取 0.1·Di
7. 尺寸文字带变量名（Ø / R / ° 前缀）
8. CDA 顶部为竖直向上翻边 + 外凸圆角 r₁；CHD 顶部为水平切口、底部外凸圆角 rs
9. 图层名 HEAD_CONTOUR/CENTER/HATCH/DIM；白色用 ACI 7
